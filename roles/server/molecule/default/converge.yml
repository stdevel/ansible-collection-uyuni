---
- name: Converge machines
  hosts: all
  become: true
  pre_tasks:
    - name: Fix FQDN
      ansible.builtin.set_fact:
        ansible_fqdn: uyuni.podman.loc

  roles:
    - role: stdevel.uyuni.server
      server_enable_monitoring: true
      # server_channels:
      #   - name: almalinux9
      #     arch: x86_64
      #   - name: almalinux9-appstream
      #     arch: x86_64
      #   - name: almalinux9-uyuni-client
      #     arch: x86_64
      # # server_release: '2023.03'
      # server_firewall_config: true
